#include <stdio.h>

/**
 * main - main block
 *
 * @argc: params
 *
 * @argv: params
 *
 * Return: 0
 *
 */

int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i > 1; i++)
	{
		printf("%s \n", argv[i]);
	}
	return (0);
}
