#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 *
 * @s: string whose length to be returned
 *
 * Return: 0
 *
 */

int _strlen(char *s)
{
	strlen(s);
	return (0);
}
