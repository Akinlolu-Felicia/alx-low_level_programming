#include <stdio.h>
/**
 * main - main block
 *
 * prints hexadecimal numbers
 *
 * Return: (0)
 *
 */
int main(void)
{	int i, y;
	i = '0';
	y = 'a';

	for (i < 10; i++;)
	{
		putchar(i);
	}
	for (y <= f; y++)
	{
		putchar(y);
	}
	putchar('\n');
	return (0);
}
